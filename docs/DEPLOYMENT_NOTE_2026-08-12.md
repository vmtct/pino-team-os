# Deployment note — 2026-08-12

The staff-link schedule flow is deployed from `main` and uses `/schedule?t=<STAFF_USERNAME>`.

This file intentionally records the deployment baseline for the production smoke verification.
