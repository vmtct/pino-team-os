import assert from"node:assert/strict";import test from"node:test";import{callWorkforceCore,type WorkforceCoreBinding}from"./workforce-core";
test("Workforce facade forwards verified identity and trusted transport context without a Staff target",async()=>{let body:unknown;let observedIp:string|undefined;const binding:WorkforceCoreBinding={async execute(request,identity,transport){body=request.body;observedIp=transport?.serverObservedIp;assert.equal(identity.subject,"verified-subject");return{status:200,body:{data:{}},requestId:"request"};},async executeWithStaffPin(){return{status:200,body:{data:{}},requestId:"staff-pin"};}};const response=await callWorkforceCore(binding,{method:"GET",path:"/profile",body:{t:"ignored"}},{provider:"cloudflare_access",subject:"verified-subject",email:"staff@example.com",issuer:"https://example.cloudflareaccess.com",audience:["tos"],expiresAt:9999999999},{serverObservedIp:"203.0.113.90"});assert.equal(response.status,200);assert.deepEqual(body,{t:"ignored"});assert.equal(observedIp,"203.0.113.90");});
