export type Staff = { id:string; name:string; email:string; phone:string; employmentStatus:string; department:string; role:string; functions:string[]; programs:string[]; appAccess:string; };
export function mapStaff(page:any):Staff { const p=page.properties; const title=p.Name?.title?.map((x:any)=>x.plain_text).join("")??""; return {id:page.id,name:title,email:p.Email?.email??"",phone:p.Phone?.phone_number??"",employmentStatus:p["Employment Status"]?.select?.name??"",department:p.Department?.select?.name??"",role:p.Role?.rich_text?.map((x:any)=>x.plain_text).join("")??"",functions:p.Functions?.multi_select?.map((x:any)=>x.name)??[],programs:p.Programs?.relation?.map((x:any)=>x.id)??[],appAccess:p["App Access"]?.select?.name??"Staff"}; }
